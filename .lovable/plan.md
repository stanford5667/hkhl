

## Update Stripe Secret Key

**Single step:** Use the `stripe--update_stripe_secret_key` tool to prompt you to paste your `sk_live_` key. This will replace the invalid `mk_` key currently stored.

No code changes needed — just the secret update.

